import Banner from './Banner';
import Navigation from './Navigation';

const Header = (props) => {

    return (
        <header>
            <Banner />
            <Navigation />
        </header>
    );
};

export default Header;