import Banner from './Banner';
import Navigation from './Navigation/Navigation';

const Header = (props) => {

    return (
        <header>
            <Banner />
            <Navigation />
        </header>
    );
};

export default Header;